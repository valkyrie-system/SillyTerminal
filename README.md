# ⚔️ Valkyrie Terminal Commands Reference

---

## 📦 Package Management

**Install commands loop `earthbound-what-battle-moment.mp3` while running and play `earthbound-you-win.mp3` on success.**

| Command | Description |
|----------|--------------|
| `:3 <pkg>` | Install (`-S`) with `cute-uwu.mp3` |
| `:33 <pkg>` | Sync + Install (`-Sy`) (`cute-uwu.mp3` ×2) |
| `:333 <pkg>` | Update + Install (`-Syu`) (`cute-uwu.mp3` ×3) |
| `:3 <pkg> q-q` | Uninstall with `lacrimosa.mp3` |
| `pls :3 <pkg>` | Sudo install (`pls.mp3` + `cute-uwu.mp3`) |
| `pls :3 <pkg> q-q` | Sudo uninstall (`pls.mp3` + `lacrimosa.mp3`) |
| `yeet` | Clean cache (interactive with sounds) |
| `pls-its-broken` | Fix pacman DB lock with `its-broken.mp3` |
| `seek` | Launch pacseek with `start-pacman.mp3` |

---

## 📂 Navigation & Flex

| Command | Description |
|----------|--------------|
| `plsgo <dir>` | Navigate (`cd`) + Clear + List (`ack.mp3`) |
| `mine <file>` | Claim ownership/List files (`mine-mine-mine.mp3`) |
| `smash <file>` | Force delete (`rm -rf`) (`smaaaash.mp3`) |
| `flex` | Show system info (`can-you-feel-my-heart.mp3`) |
| `touch-grass` | Exit terminal (`metal-pipe-falling.mp3`) |

---

## 🔧 System Commands

| Command | Description |
|----------|--------------|
| `lets-go-gambling` | Update all with gambling sounds + Earthbound music loop |
| `gotta-go-fast` | Update mirrors with Sonic X Theme loop (Ends with `haha-ha-one`) |
| `fuckoff` | Shutdown with `fahhhhhhhhhhhhhhh.mp3` |
| `:P` | Restart with `lizard-button.mp3` (×4) |

---

## 🎛 Utility

| Command | Description |
|----------|--------------|
| `pls <cmd>` | Run with sudo + `pls.mp3` |
| `pls <cmd> iforgor` | Show sudo help + `no-i-forgot.mp3` |
| `iforgor <cmd>` | Show command help + `no-i-forgot.mp3` |
| `sound-volume <0-100>` | Adjust all sound volumes globally |
| `with-sounds <cmd>` | Run any interactive command with y/n sounds |
| `iforgor help` | Show this menu + `no-i-forgot.mp3` |

---

## 🔊 Sounds & Keybinds

| Action | Sound / Effect |
|---------|----------------|
| Terminal Start | `oo-ee-ee-aa.mp3` |
| Terminal Exit | `metal-pipe-falling.mp3` |
| Update Success | `lets-go-gambling-win.mp3` / `earthbound-you-win.mp3` |
| Update Failure | `aw-dang-it.mp3` |
| Flatpak EOL | `aw-dang-it.mp3` (Repeats per warning) |
| Help / Iforgor | `no-i-forgot.mp3` |
| **Ctrl+Y** | Append `\| yes` + dog-clicker |
| **Ctrl+N** | Append `&& no` + `vine-boom.mp3` |
| **Ctrl+B** | Play `vine-boom.mp3` + type `n` |
| **Live Typing** | Triggers specific sounds for `smash`, `mine`, `:3`, etc. |

---

✨ *System tuned for chaos — Valkyries approved.*
